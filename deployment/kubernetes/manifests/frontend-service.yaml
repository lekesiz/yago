apiVersion: v1
kind: Service
metadata:
  name: yago-frontend
  namespace: yago
  labels:
    app: yago
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: yago
    component: frontend
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
